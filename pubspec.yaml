name: new_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  syncfusion_flutter_charts: ^18.2.45
  sqflite: ^1.3.1
  path_provider: ^1.6.11
  syncfusion_flutter_datepicker: ^18.2.45-beta
  flutter_date_pickers: ^0.1.4
  date_range_picker: ^1.0.6
  intl: ^0.16.1
  month_picker_dialog: ^0.3.2
  custom_splash: ^0.0.2
  firebase_auth: ^0.16.1
  shared_preferences: ^0.5.8
  after_layout: ^1.0.7+2
  persistent_bottom_nav_bar: ^2.0.5
  weather_icons: ^2.0.1
  dio: ^3.0.9

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/debswanaLogo.png
